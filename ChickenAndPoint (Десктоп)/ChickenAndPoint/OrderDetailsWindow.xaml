<Window x:Class="ChickenAndPoint.OrderDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChickenAndPoint"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Детали Заказа" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{materialDesign:MaterialDesignFont}" Icon="/logo3.png">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock x:Name="WindowTitleTextBlock" Text="Состав заказа №" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="10"/>
            <TextBlock x:Name="StatusTextBlock" Text="Загрузка..." HorizontalAlignment="Center" Margin="20" Visibility="Collapsed"/>
            <DataGrid x:Name="OrderItemsDataGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          Margin="10"
                          materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                <DataGrid.Columns>
                    <materialDesign:DataGridTextColumn Header="Название Блюда" Binding="{Binding НазваниеБлюда}" Width="*"/>
                    <materialDesign:DataGridTextColumn Header="Количество" Binding="{Binding Количество}" Width="Auto"/>
                    <materialDesign:DataGridTextColumn Header="Цена (на момент заказа)" Binding="{Binding ЦенаНаМоментЗаказа, StringFormat=C}" Width="Auto"/>
                    <materialDesign:DataGridTextColumn Header="Сумма" Binding="{Binding СуммаПозиции, StringFormat=C}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>